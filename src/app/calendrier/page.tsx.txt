'use client'

import { useState } from 'react'
import Sidebar from '@/components/layout/Sidebar'
import TopBar from '@/components/layout/TopBar'
import Image from 'next/image'

const days = ['Lun 13', 'Mar 14', 'Mer 15', 'Jeu 16', 'Ven 17', 'Sam 18', 'Dim 19']
const hours = ['08:00', '09:00', '10:00', '11:00', '12:00', '13:00', '14:00', '15:00', '16:00', '17:00']

const appointments = [
  {
    id: 1,
    day: 0, // Lundi
    startHour: 1, // 09:00
    duration: 2,
    title: 'Consultation initiale',
    patient: 'Sarah Jenkins',
    colorClass: 'bg-teal-50 border-teal-500 text-teal-800',
  },
  {
    id: 2,
    day: 1, // Mardi
    startHour: 3, // 11:00
    duration: 2,
    title: 'Récupération post-op',
    patient: 'Sarah Jenkins',
    colorClass: 'bg-primary-container text-white border-on-primary-container',
    featured: true,
  },
  {
    id: 3,
    day: 3, // Jeudi
    startHour: 6, // 14:00
    duration: 2,
    title: 'Massage sportif',
    patient: 'Marc Dupond',
    colorClass: 'bg-amber-50 border-amber-500 text-amber-800',
  },
]

export default function CalendrierPage() {
  const [drawerOpen, setDrawerOpen] = useState(true)

  return (
    <div className="flex min-h-screen bg-background">
      <Sidebar />
      <div className="flex-1 ml-64 flex flex-col min-h-screen">
        <TopBar searchPlaceholder="Rechercher un patient ou un RDV..." />

        <div className="flex flex-1 overflow-hidden">
          {/* Main calendar area */}
          <section className="flex-1 overflow-auto">
            {/* Toolbar */}
            <div className="sticky top-0 z-30 bg-white/90 backdrop-blur-sm border-b border-slate-200 px-6 py-3 flex justify-between items-center">
              <div className="flex items-center gap-4">
                <div className="flex items-center gap-2">
                  <button className="w-8 h-8 rounded-lg border border-slate-200 flex items-center justify-center hover:bg-slate-50 transition-colors">
                    <span className="material-symbols-outlined text-sm">chevron_left</span>
                  </button>
                  <h3 className="font-title-sm text-on-surface px-2">
                    Semaine du 13 – 19 Novembre 2023
                  </h3>
                  <button className="w-8 h-8 rounded-lg border border-slate-200 flex items-center justify-center hover:bg-slate-50 transition-colors">
                    <span className="material-symbols-outlined text-sm">chevron_right</span>
                  </button>
                </div>
              </div>
              <div className="flex items-center gap-3">
                <div className="flex rounded-lg border border-slate-200 overflow-hidden text-sm">
                  {['Jour', 'Semaine', 'Mois'].map((v, i) => (
                    <button
                      key={v}
                      className={`px-3 py-1.5 font-semibold transition-colors ${i === 1 ? 'bg-primary text-white' : 'text-slate-600 hover:bg-slate-50'}`}
                    >
                      {v}
                    </button>
                  ))}
                </div>
                <button className="btn-primary text-sm px-4 py-2">
                  <span className="material-symbols-outlined text-sm">add</span>
                  Nouveau RDV
                </button>
              </div>
            </div>

            {/* Calendar Grid */}
            <div className="min-w-[700px]">
              {/* Day headers */}
              <div className="calendar-grid border-b border-slate-200 bg-white sticky top-[57px] z-20">
                <div className="p-3 border-r border-slate-100" />
                {days.map((d, i) => (
                  <div
                    key={d}
                    className={`p-3 text-center border-r border-slate-100 last:border-r-0 ${
                      i === 1 ? 'bg-primary/5' : ''
                    }`}
                  >
                    <p className="text-xs text-slate-500 font-semibold uppercase tracking-wider">
                      {d.split(' ')[0]}
                    </p>
                    <p
                      className={`text-lg font-bold mt-0.5 ${
                        i === 1 ? 'text-primary' : 'text-on-surface'
                      }`}
                    >
                      {d.split(' ')[1]}
                    </p>
                  </div>
                ))}
              </div>

              {/* Time rows */}
              <div className="relative">
                {hours.map((h, hi) => (
                  <div key={h} className="calendar-grid border-b border-slate-100 min-h-[80px]">
                    <div className="p-3 border-r border-slate-100 flex items-start justify-end pr-3">
                      <span className="text-[11px] text-slate-400 font-semibold">{h}</span>
                    </div>
                    {days.map((d, di) => (
                      <div
                        key={d}
                        className={`border-r border-slate-100 last:border-r-0 hover:bg-slate-50/30 transition-colors cursor-pointer ${
                          di === 1 ? 'bg-primary/5' : ''
                        }`}
                      />
                    ))}
                  </div>
                ))}

                {/* Appointment overlays */}
                <div className="absolute inset-0 calendar-grid pointer-events-none">
                  <div /> {/* time col spacer */}
                  {days.map((_, di) => (
                    <div key={di} className="relative">
                      {appointments
                        .filter((a) => a.day === di)
                        .map((a) => (
                          <div
                            key={a.id}
                            className={`absolute left-1 right-1 border-l-4 p-2 pointer-events-auto cursor-pointer shadow-sm rounded-r transition-transform hover:scale-[1.02] ${
                              a.featured ? 'shadow-md ring-2 ring-white scale-105 z-10' : ''
                            } ${a.colorClass}`}
                            style={{
                              top: `${a.startHour * 80 + 4}px`,
                              height: `${a.duration * 80 - 8}px`,
                            }}
                          >
                            <p className="text-[10px] font-bold truncate">{a.title}</p>
                            <p className="text-[9px] opacity-80 mt-0.5">{a.patient}</p>
                          </div>
                        ))}
                    </div>
                  ))}
                </div>

                {/* Current time indicator */}
                <div
                  className="absolute left-0 right-0 h-[2px] bg-error z-20 flex items-center pointer-events-none"
                  style={{ top: '280px' }}
                >
                  <div className="w-3 h-3 rounded-full bg-error ml-20 -translate-x-1/2" />
                </div>
              </div>
            </div>
          </section>

          {/* Patient Drawer */}
          {drawerOpen && (
            <aside className="w-96 border-l border-slate-200 bg-white overflow-y-auto flex flex-col flex-shrink-0">
              <div className="p-6 border-b border-slate-100 flex justify-between items-center bg-surface-container-lowest">
                <h3 className="font-title-sm text-primary">Profil du Patient</h3>
                <button
                  onClick={() => setDrawerOpen(false)}
                  className="text-slate-400 hover:text-slate-600 transition-colors"
                >
                  <span className="material-symbols-outlined">close</span>
                </button>
              </div>

              {/* Patient overview */}
              <div className="p-8 text-center border-b border-slate-100">
                <div className="relative inline-block">
                  <Image
                    src="https://lh3.googleusercontent.com/aida-public/AB6AXuDjkJ89w0UuCCXtAryuekh3-qd6Ng1PxZUEY0OO1Kbx3CK3GnBXD59v8ctcHnbFbiHN2f8CRNRhQIs3t0iYgNm_vjZu0pY8UOesgS7QaDbCxNMmSfawTvxNukp1t9S1DOIqrjcBMWNtiLc-69RlLsdTKmWxnzNDZK_rIu0Dd3nScd-t5gFNt8eNuOddnt0wyix0TGeEqMzWZf4raAY-1_iqaSjlyJFYSZNtmDq3SqVV3D45_kJFwtFgksRU4rEk7_PsABemQIHXBss"
                    alt="Sarah Jenkins"
                    width={96}
                    height={96}
                    className="w-24 h-24 rounded-full mx-auto border-4 border-white shadow-lg object-cover"
                  />
                  <span className="absolute bottom-1 right-1 w-5 h-5 bg-green-500 border-2 border-white rounded-full" />
                </div>
                <h4 className="mt-4 font-headline-md text-primary">Sarah Jenkins</h4>
                <div className="flex items-center justify-center gap-2 mt-2">
                  <span className="bg-secondary-container text-on-secondary-container text-[11px] font-bold px-2 py-0.5 rounded-full">
                    ACTIF
                  </span>
                  <span className="text-slate-400 text-xs">•</span>
                  <span className="text-on-surface-variant text-xs">ID: #4492-SJ</span>
                </div>
              </div>

              <div className="px-6 py-6 space-y-6">
                {/* Medical summary */}
                <div className="bg-surface-container-low rounded-xl p-5 border border-slate-100">
                  <div className="flex items-center gap-2 mb-3">
                    <span className="material-symbols-outlined text-primary text-lg">medical_information</span>
                    <h5 className="text-label-caps text-primary uppercase">Résumé Médical</h5>
                  </div>
                  <div className="space-y-3">
                    {[
                      { label: 'Diagnostic', value: 'Ligament croisé (LCA)' },
                      { label: 'Chirurgie', value: '12 Avril 2024' },
                      { label: 'Progression', value: '65% Récupéré', highlight: true },
                    ].map((item) => (
                      <div key={item.label} className="flex justify-between">
                        <span className="text-xs text-on-surface-variant">{item.label}</span>
                        <span className={`text-xs font-semibold ${item.highlight ? 'text-teal-600' : 'text-primary'}`}>
                          {item.value}
                        </span>
                      </div>
                    ))}
                    <div className="w-full bg-slate-200 h-1.5 rounded-full mt-2">
                      <div className="bg-teal-500 h-1.5 rounded-full w-[65%]" />
                    </div>
                  </div>
                </div>

                {/* Prescribed exercises */}
                <div>
                  <div className="flex justify-between items-center mb-4">
                    <h5 className="text-label-caps text-primary uppercase">Exercices Prescrits</h5>
                    <button className="text-[10px] text-teal-600 font-bold hover:underline">Voir tout</button>
                  </div>
                  <div className="space-y-3">
                    {[
                      { name: 'Extensions de jambe', detail: '3 séries x 15 reps' },
                      { name: 'Équilibre sur Bosu', detail: '5 min de maintien' },
                    ].map((ex) => (
                      <div
                        key={ex.name}
                        className="flex items-center gap-4 p-3 bg-white border border-slate-100 rounded-lg hover:shadow-sm transition-shadow"
                      >
                        <div className="w-10 h-10 rounded-lg bg-slate-100 flex-shrink-0 flex items-center justify-center">
                          <span className="material-symbols-outlined text-slate-400 text-base">fitness_center</span>
                        </div>
                        <div className="flex-1">
                          <p className="text-xs font-bold text-primary">{ex.name}</p>
                          <p className="text-[10px] text-on-surface-variant">{ex.detail}</p>
                        </div>
                        <span className="material-symbols-outlined text-teal-500 text-sm">play_circle</span>
                      </div>
                    ))}
                  </div>
                </div>

                {/* Notes */}
                <div className="bg-tertiary-container/5 rounded-xl p-4 border border-tertiary/10">
                  <div className="flex items-center gap-2 mb-2">
                    <span className="material-symbols-outlined text-tertiary">event_note</span>
                    <h5 className="text-[11px] font-bold text-tertiary uppercase tracking-wider">
                      Notes du praticien
                    </h5>
                  </div>
                  <p className="text-[11px] text-tertiary/80 italic leading-relaxed">
                    &ldquo;Alerte : légère douleur signalée lors de l&apos;extension complète. Réduire l&apos;amplitude
                    de 5% pour la prochaine séance.&rdquo;
                  </p>
                </div>

                <button className="w-full btn-primary">
                  <span className="material-symbols-outlined text-sm">add_circle</span>
                  Nouveau Compte Rendu
                </button>
              </div>
            </aside>
          )}
        </div>
      </div>

      {/* FAB */}
      <button
        className={`fixed bottom-8 z-30 w-14 h-14 bg-primary text-white rounded-full shadow-2xl flex items-center justify-center hover:scale-105 active:scale-95 transition-all ${
          drawerOpen ? 'right-[416px]' : 'right-8'
        }`}
        onClick={() => setDrawerOpen(true)}
      >
        <span className="material-symbols-outlined text-2xl">add</span>
      </button>
    </div>
  )
}
